#ifndef __HELPER__FUNTIONS__HEADER
#define __HELPER__FUNTIONS__HEADER

#include <iostream>

bool isWhitespace(const char);

size_t countWords(const char*);

int myStricmp(const char*, const char*);

bool doesFileExits(const char*);

#endif
